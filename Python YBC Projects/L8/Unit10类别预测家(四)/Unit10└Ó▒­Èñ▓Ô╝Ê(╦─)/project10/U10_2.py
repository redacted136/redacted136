from sklearn.svm import SVC
from src_2 import main

class Machinelearning():

    def __init__(self):
        self.name = "非线性支持向量机模型"

    def model_use(self, train_data, train_label, new_data):
        # 实例化模型，设置非线性核。

        # 将数据与标签输入模型进行训练

        # 模型对新数据的类别进行预测

        return result

ml = Machinelearning()
main(ml)
